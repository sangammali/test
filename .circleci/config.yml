version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "e3:8d:85:21:b7:ce:1d:48:2e:24:cd:29:1d:0a:61:99"
      - run:
          name: Git Pull on Windows EC2
          command: |
            # Replace these variables with your actual values
            instanceIp="3.110.83.18"
            username="Administrator"
            
            # Run Git Pull command on the remote Windows instance using SSH and Git Bash
            ssh -o StrictHostKeyChecking=no $username@$instanceIp 'cd C:\Users\Administrator\Desktop\sangam> && git pull'

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
